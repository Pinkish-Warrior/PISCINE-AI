# AI.GO Beginner Roadmap

A step-by-step path through the AI.GO curriculum (01-edu), built for students with **zero prior programming experience**. Table view of [`ROADMAP.md`](ROADMAP.md).

Haven't done the one-time setup yet? Go do [`SETUP.md`](SETUP.md) first, then come back here.

---

## Phase 1 — Foundations: HTML and CSS

Goal: get comfortable making things appear, move, and react on a page.

| # | Exercise | What you'll learn |
|---|----------|-------------------|
| 1 | `first-hello` | Show/hide text with a button — your first HTML + JS interaction |
| 2 | `first-move` | Animate the robot with CSS transforms |
| 3 | `first-wink` | Build simple interactive animations |
| 4 | `first-function` | Write your first JavaScript function |

**Checkpoint:** Can you explain, in your own words, what a CSS transform does and how a button click triggers JS code?

---

## Phase 2 — Core JavaScript Concepts

Goal: learn the building blocks every program is made of — variables, functions, conditionals, arrays.

| # | Exercise | What you'll learn |
|---|----------|-------------------|
| 5 | `declare-everything` | Variable declaration & scope |
| 6 | `play-with-variables` | Data types and manipulating values |
| 7 | `i-win-arguments` | Function parameters and arguments |
| 8 | `good-recipe` | Writing clean, well-organized functions |
| 9 | `only-if` | Conditional logic (if/else) |
| 10 | `listed` | Arrays and lists |

**Checkpoint:** Can you write a function that takes an argument, checks a condition, and loops over a list?

---

## Phase 3 — DOM Manipulation and Events

Goal: connect JavaScript to the page itself — read it, change it, style it dynamically.

| # | Exercise | What you'll learn |
|---|----------|-------------------|
| 11 | `objects-around` | JavaScript objects and properties |
| 12 | `call-it` | Function invocation and callbacks |
| 13 | `class-it` | Dynamic CSS classes |
| 14 | `select-then-style` | Selecting DOM elements and styling them |
| 15 | `colorful-arms` | CSS animations and transformations |
| 16 | `colorful-legs` | Advanced styling techniques |

**Checkpoint:** Can you select an element with JS and change its style/class based on user interaction?

---

## Phase 4 — Advanced Concepts

Goal: combine everything into more complex, stateful, multi-part programs.

| # | Exercise | What you'll learn |
|---|----------|-------------------|
| 17 | `embedded-organs` | Nested elements & complex DOM structures |
| 18 | `glance-on-power` | Operators and expressions |
| 19 | `robots-harmony` | Managing state across multiple objects |
| 20 | `transform-objects` | Object transformation methods |
| 21 | `star-forge` | Complex animations and effects |
| 22 | `the-skeleton` | Semantic HTML structure |
| 23 | `the-smooth-operator` | Animation timing and smoothing |
| 24 | `JSPowered Mode` | Advanced JavaScript, capstone-style |

**Checkpoint:** Can you build a small feature end-to-end — HTML structure, CSS styling, JS behavior — without a template?

---

## Suggested Pace

- **1 exercise/day** for a relaxed pace → ~1 month total
- **2 exercises/day** for an intensive pace → ~2 weeks total
- Don't move to the next phase until you can explain *why* your solution works, not just that it passes.

## Study Tips for Beginners

1. **Read before you code** — understand the exercise's goal before touching the editor.
2. **Break things on purpose** — change a value, see what happens. This builds intuition faster than reading docs.
3. **Use browser dev tools** — `console.log()` everything while learning; inspect the DOM live.
4. **Don't skip the "boring" phase 2 exercises** — variables, conditionals, and arrays are used in every single exercise after them.
5. **Revisit earlier exercises** after finishing later ones — they'll make more sense in hindsight.

